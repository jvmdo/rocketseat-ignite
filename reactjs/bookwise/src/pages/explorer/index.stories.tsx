import { Meta, StoryObj } from '@storybook/react'
import { Explorer, ExplorerProps } from '.'

export default {
  title: 'Pages / Explorer',
  component: Explorer,
} as Meta<ExplorerProps>

export const Default = {} as StoryObj<ExplorerProps>
