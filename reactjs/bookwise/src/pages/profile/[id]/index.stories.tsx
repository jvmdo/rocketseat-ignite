import { Meta, StoryObj } from '@storybook/react'
import Profile, { ProfileProps } from './index.page'

export default {
  title: 'Pages / Profile',
  component: Profile,
} as Meta<ProfileProps>

export const Default = {} as StoryObj<ProfileProps>
