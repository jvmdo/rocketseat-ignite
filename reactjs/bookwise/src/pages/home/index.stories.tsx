import { Meta, StoryObj } from '@storybook/react'
import Home, { HomeProps } from './index.page'

export default {
  title: 'Pages / Home',
  component: Home,
} as Meta<HomeProps>

export const Default = {} as StoryObj<HomeProps>
