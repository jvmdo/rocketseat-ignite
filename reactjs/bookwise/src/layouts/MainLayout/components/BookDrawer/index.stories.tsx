import { Meta, StoryObj } from '@storybook/react'
import { BookDrawer } from '.'

export default {
  title: 'Main Layout / BookDrawer',
  component: BookDrawer,
} as Meta

export const Default = {} as StoryObj
